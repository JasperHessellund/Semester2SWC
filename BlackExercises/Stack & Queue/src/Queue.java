import java.util.Arrays;
import java.util.Objects;
import java.util.function.Supplier;

/**
 * Created by jasperhessellund on 14/11/2016.
 */
public class Queue <E>{/*
    private final Supplier<? extends E> Queue1;
    private E[] queueArray;
    private int queueSize;
    private int first, items, end = 0;

    public Queue(Supplier<? extends E> queue, int size) {
        Queue = queue;
        queueSize = size;

        queueArray = new E[size];
        Arrays.fill(queueArray, "null");
    }
    public void add (String input) throws Exception {
        if (items+1<=queueSize) {
            queueArray[end] = Objects.requireNonNull(Queue1);
            end++;
            items++;

        } else throw new Exception("Queue is full");
    }

    public void remove() throws Exception {
        if (items >0) {
            queueArray[first] = "null";
            first++;
            items--;
        } else throw new Exception("Queue is empty");
    }
    public void peek () {
        System.out.println(queueArray[first]);
    }

*/
}
